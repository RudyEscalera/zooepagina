<p id="notice"><%= notice %></p>

<p>
  <strong>Ci:</strong>
  <%= @persona.ci %>
</p>

<p>
  <strong>Nombres:</strong>
  <%= @persona.nombres %>
</p>

<p>
  <strong>Apellido paterno:</strong>
  <%= @persona.apellido_paterno %>
</p>

<p>
  <strong>Apellido materno:</strong>
  <%= @persona.apellido_materno %>
</p>

<p>
  <strong>Fecha nacimiento:</strong>
  <%= @persona.fecha_nacimiento %>
</p>

<hr>
<h3>MASCOTAS ADOPTADAS</h3>
<br>
<%= link_to '+Registrar Adopcion', nuevo_adop_path, :class => 'btn btn-info' %>

<br>
<BR>
<br>
NUMERO DE ADOPCIONES:: <%= @adopciones.size %>
<br>
<% @adopciones.each do |adopcion| %>
  ID ADOPCION::<%= adopcion.id %> <BR>
  # DE ADULTOS::<%= adopcion.numero_adultos %> <BR>
  # DE ninios::<%= adopcion.numero_ninios %> <BR>
  # DE PERROS::<%= adopcion.numero_perros %> <BR>
  # DE GATOS::<%= adopcion.numero_gatos %> <BR>
  # DE OTROS::<%= adopcion.numero_otros %> <BR>
  REFERENCIA DE LA CASA::<%= adopcion.referencia_casa %> <BR>
  NUMERO 1 DE REFERENCIA DE LA CASA::<%= adopcion.numero_referencia1 %> <BR>
  NUMERO 2 DE REFERENCIA DE LA CASA::<%= adopcion.numero_referencia2 %> <BR>
  ID MASCOTA::<%= adopcion.id_mascota %> <BR>

  <h3>MASCOTA</h3>
  <!-- obtengo la mascota -->
  <% @mascotas.each do |mascota| %>
    <% if adopcion.id_mascota == mascota.id %>
      ID::<%= mascota.id %><BR>
      NOMBRE::<%= mascota.nombre %><BR>
      ESPECIE::<%= mascota.especie %><BR>
      RAZA::<%= mascota.raza %><BR>
      EDAD MESES:::<%= mascota.edad %><BR>
      COLOR::<%= mascota.color %><BR>
    <%end%>
  <%end%>

<%end%>

<%= link_to '<< Atras', personas_path, :class => 'btn btn-default' %>
<%= link_to 'Editar', edit_persona_path(@persona), :class => 'btn btn-warning' %> 
