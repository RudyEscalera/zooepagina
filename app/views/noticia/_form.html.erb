<%= form_for(@noticium) do |f| %>
  <% if @noticium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noticium.errors.count, "error") %> prohibited this noticium from being saved:</h2>

      <ul>
      <% @noticium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-md-5">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo, :class => "form-control input" %>
  </div>
  <div class="field col-md-5">
    <%= f.label :categoria %><br>
    <%= f.text_field :categoria, :class => "form-control input" %>
  </div>
  <div class="field col-md-5">
    <%= f.label :contenido %><br>
    <%= f.text_area :contenido, :class => "form-control input" %>
  </div>
  <div class="actions">
    <%= f.submit :Guardar, :class => 'btn btn-success' %>
  </div>
<% end %>
